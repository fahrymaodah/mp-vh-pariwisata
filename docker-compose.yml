services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: par-hotel-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - /var/www/html/vendor
      - /var/www/html/node_modules
    networks:
      - par-hotel-network
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started

  nginx:
    image: nginx:alpine
    container_name: par-hotel-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/sites:/etc/nginx/conf.d
      - ./docker/nginx/ssl:/etc/nginx/ssl
    networks:
      - par-hotel-network
    depends_on:
      - app

  mysql:
    image: mysql:8.4
    container_name: par-hotel-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: par_hotel
      MYSQL_USER: par_hotel
      MYSQL_PASSWORD: par_hotel_secret
      MYSQL_ROOT_PASSWORD: root_secret
    volumes:
      - mysql-data:/var/lib/mysql
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/my.cnf
    ports:
      - "3307:3306"
    networks:
      - par-hotel-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot_secret"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:alpine
    container_name: par-hotel-redis
    restart: unless-stopped
    volumes:
      - redis-data:/data
    ports:
      - "6380:6379"
    networks:
      - par-hotel-network

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: par-hotel-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_USER: root
      PMA_PASSWORD: root_secret
    ports:
      - "8080:80"
    networks:
      - par-hotel-network
    depends_on:
      - mysql

networks:
  par-hotel-network:
    driver: bridge

volumes:
  mysql-data:
  redis-data:
